{% extends 'base.html.twig' %}

{% block title %}Events{% endblock %}

{% block body %}
    <h1>Events</h1>

    {{ event.id }}
    {{ event.idMarker }}
    {{ event.title }}
    {{ event.description | raw }}


{% endblock %}
